[package]
name = "fraud-detect"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
common-utils = { path = "../common-utils" }
soroban-sdk = { workspace = true }

[dev-dependencies]
credit-score = { path = "../credit-score" }
soroban-sdk = { workspace = true, features = ["testutils"] }
