# examples

Sample integrations against chenaikit. During migration, drop any example
that duplicates `apps/frontend` screens rather than demonstrating a distinct
integration pattern (SDK usage, CLI usage, oracle-node usage, etc).
